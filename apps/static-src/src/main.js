// Import HTMX for dynamic HTML loading
import 'htmx.org';

// Import Alpine.js for interactive components
import Alpine from 'alpinejs';

// Import Skeleton UI + Wintry theme styles
import './main.css';

// Initialize Alpine.js
window.Alpine = Alpine;
Alpine.start();
